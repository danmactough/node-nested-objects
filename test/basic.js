describe('basic', function () {
  it('can get nested a nested property');
  it('can set nested a nested property');
  it('can delete nested a nested property');
  it('can flatten a nested object');
});